# Reference only; the application does not automatically load this file.
# Set a private credential file OR OPENROUTER_API_KEY in your environment.
OPENROUTER_API_KEY_FILE=/path/to/private/openrouter.key
# Official API: pass --provider typesafe (only that provider's key is read).
# TYPESAFE_API_KEY_FILE=/path/to/private/typesafe.key
# Or export TYPESAFE_API_KEY instead of using a file.
LIBERO_ROOT=/path/to/LIBERO
MUJOCO_GL=egl
# Optional: JEV_LIBERO_CONFIG_DIR=/path/to/app-owned/libero-config
